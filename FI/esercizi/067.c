/*
Definire un tipo di dato strutturato in C per rappresentare una frazione in
termini di numeratore e denominatore (due numeri interi).
*/

#include <stdio.h>

typedef struct frac_s {
  int num, den;
} frac_t;

int main(int argc, char** argv) { return 0; }
